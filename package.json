{
  "name": "and-relax",
  "version": "0.1.0",
  "description": "A new and wonderful project",
  "main": "index.js",
  "scripts": {
    "clean": "mkdir -p ./dist && rm -rf ./dist/*",
    "build": "npm run clean && browserify ./index.js --standalone ANDRELAX -t babelify -o ./dist/and-relax.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "babel": {
    "presets": ["es2015"]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.1.18",
    "babelify": "^7.2.0",
    "browserify": "^12.0.1"
  }
}
